# Subflow: Navigate to cart screen

appId: com.ennio.example

---

# Pop any Stack-pushed screen so the tab bar is reachable.
- back

- tapOn:
    text: "Cart"
- assertVisible:
    anyOf:
      - id: "cart-screen"
      - id: "cart-screen-empty"
    timeout: 5000

# Subflow: Navigate to products screen

appId: com.ennio.example

---

# Pop any Stack-pushed screen so the tab bar is reachable.
- back

- tapOn:
    text: "Products"
- assertVisible:
    id: "products-screen"

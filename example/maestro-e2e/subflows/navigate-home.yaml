# Subflow: Navigate to home screen
# Ensures we're on the home screen regardless of current state

appId: com.ennio.example

---

- tapOn:
    text: "Home"
- assertVisible:
    id: "home-screen"
    timeout: 3000

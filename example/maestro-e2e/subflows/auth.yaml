# Subflow: Authentication helpers

appId: com.ennio.example

---
# Demo login — conditional so subflow no-ops when already logged in.
- runFlow:
    when:
      visible:
        id: 'demo-login-btn'
    commands:
      - tapOn:
          id: 'demo-login-btn'

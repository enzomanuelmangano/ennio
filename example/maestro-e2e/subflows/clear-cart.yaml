# Subflow: Clear cart if not empty

appId: com.ennio.example

---
- runFlow:
    file: navigate-cart.yaml

# Optional cleanup — clear-cart-btn only present when cart has items.
- tapOn:
    id: 'clear-cart-btn'
    optional: true
- tapOn:
    text: 'Clear'
    optional: true

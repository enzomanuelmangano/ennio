# Dependencies
node_modules/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build outputs
lib/
dist/
*.tsbuildinfo

# Ennio prebuilt dylibs — produced by CI on tag push, shipped in the npm
# tarball, never committed. Local devs run packages/ennio/scripts/build-shim.sh
# + packages/ennio/scripts/build-dylib.sh to regenerate; manifest.json is
# refreshed by scripts/regen-manifest.sh.
packages/ennio/prebuilt/*.dylib
packages/ennio/prebuilt/manifest.json

drivers
reports

# Nitro generated
packages/nitro/nitrogen/generated/

# iOS
*.xcworkspace
*.xcuserdata
Pods/
DerivedData/

# Stale root-level prebuild output. The active Expo project lives at
# example/, so /ios and /android at repo root are vestigial. Anchor
# with a leading slash so example/ios stays tracked.
/ios/
/android/

# Android
*.apk
*.aab
.gradle/
build/
local.properties
.cxx/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Turbo
.turbo/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tgz
node_modules

# Claude Code harness state
.claude/scheduled_tasks.lock

# Local debug screenshots
packages/cli/probe-*
